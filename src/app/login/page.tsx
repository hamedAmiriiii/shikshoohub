import CustomerLoginPageContent from "@/app/componentsShop/customer/CustomerLoginPageContent";

/** ورود مشتری (خریدار) — جدا از /admin/login */
export default function CustomerLoginPage() {
  return <CustomerLoginPageContent />;
}
